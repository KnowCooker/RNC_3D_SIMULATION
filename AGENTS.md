# RNC 工程协作规则

适用于本仓库内任何开发者和 AI。

1. 开始工作前先读 `开发记录.md`，再读本文件、`docs/plan/00_两周开发总计划.md`、所属组需求、`03_每日任务与验收.md`、`04_接口与AI执行指南.md` 和 `src/shared/contracts.ts`。
2. 用户已于2026-09-24明确恢复完整目标，见docs/plan/10_FULL_GOAL.md：四类SUV、车辆交互/剖面、可改制参考与扬声器、参数化路噪与空间声场。旧demo-v2及其fixture保持冻结作为回归基准；新增lab-v3按完整目标扩展。教学模型不得冒充实车标定或有限元结果。
3. A 组只改 `src/team-a/**` 与自己的测试；B 组只改 `src/team-b/**` 与自己的测试。需要共管文件时先在开发记录中写明原因与接口影响，由 A1 + B1 协调；已有明确跨组任务授权时直接按授权执行。
4. shared、integration、根配置、CI 和公共脚本为共管。A/B 不可直接互相导入；装配只在 integration 中进行。
5. 不改 fixtures/reference 中的既有基准，不放宽容差来通过测试，不硬编码降噪，不以假数据冒充计算结果。保留 16 组 W、16 条 S 和完整交叉影响。
6. **每个代码修改批次完成后，立即同步根目录开发记录；代码和记录必须在同一个提交。** 更新当前状态、改动、验证、下一步、跨组影响及阻塞。未跑、失败、人工待测必须如实写明。
7. 收尾至少执行所属组测试、`pnpm typecheck`、`pnpm check:boundaries`；提交前执行 `pnpm check`。数值改变需完整对照。不要声称未执行的检查已通过。
8. 提交与推送前检查 git status / git diff。只提交本任务改动；不强推、不覆盖他人提交。新开发默认用所属组分支和 PR。开发记录冲突保留双方条目，重新核对顶部状态。
9. 交接报告列出修改文件、实际验证、未完成项、下一步、提交 SHA 与远端同步结果。无法推送时明确“仅本地，GitHub 尚未同步”。
10. A 组由两个独立 Codex 工作区协作。启动时执行 `docs/coordination/README.md` 的同步、角色确认和远端任务检查；读取 A1/A2 交接页，只认领所属角色的一个任务。角色不明确时先读取本地 `git config --local rnc.role`；仍不明确才询问，不自动都选 A1。
11. main 记录已集成进度；未合并任务以原仓库的开放 PR 及其最新分支交接记录为准。只读 main 或本地日志不能判断无人开发。新功能修改前先发布任务认领提交和草稿 PR；无法发布时明确认领尚未生效，不宣称已占用远端任务。

记录格式见 docs/协作开发指南.md。本地钩子通过 `pnpm setup` 安装；不要绕过记录检查。
