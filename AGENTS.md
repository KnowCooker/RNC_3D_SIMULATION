# RNC 工程协作规则

适用于本仓库内任何开发者和 AI。

## 调试前先确认本次用户身份

- 每位用户开始一次新的开发/调试会话，先确认其本次角色为 A1、A2、B1 或 B2。用户已在本会话明确声明身份时直接采用，不重复询问；同一用户、同一身份的连续任务可沿用本会话确认。
- 用户未主动说明角色时，必须先问：“请确认你本次的开发身份：A1、A2、B1 还是 B2？”收到明确回答后才能认领任务、调试、修改、登记负责人或提交。等待期间只可读取状态和准备不依赖身份的信息。
- 不得根据 GitHub 账号、Git author、分支名、历史日志、本地 `rnc.role` 或上一位使用者的身份自动认定当前用户。新会话、换人或角色不清时重新询问；只说“我是B组”仍需明确 B1/B2。明确声明身份不要求提供真实姓名或凭据。
- 本地 `rnc.role` 只记录最近一次确认，不能代替本次确认。不要修改 Git author 来冒充某位开发者；在开发记录、调试/验收日志、Git 提交说明和 PR 中独立记录角色、任务 ID 和实际执行者（如 Codex 代 B1 执行）。

1. 开始工作前先读 `开发记录.md`，再读本文件、`docs/plan/10_FULL_GOAL.md`、`docs/coordination/A_FINAL_DELIVERY.md`、所属角色交接及 `src/shared/lab-contracts.ts`。00～04两周计划及 `contracts.ts` 是demo-v2历史基准，维护旧版时读取；其中缩减范围不能覆盖完整目标。
2. 用户已于2026-09-24明确恢复完整目标，见docs/plan/10_FULL_GOAL.md：四类SUV、车辆交互/剖面、可改制参考与扬声器、参数化路噪与空间声场。旧demo-v2及其fixture保持冻结作为回归基准；新增lab-v3按完整目标扩展。教学模型不得冒充实车标定或有限元结果。
3. A 组只改 `src/team-a/**` 与自己的测试；B 组只改 `src/team-b/**` 与自己的测试。需要共管文件时先在开发记录中写明原因与接口影响，由 A1 + B1 协调；已有明确跨组任务授权时直接按授权执行。
4. shared、integration、根配置、CI 和公共脚本为共管。A/B 不可直接互相导入；装配只在 integration 中进行。
5. 不改 fixtures/reference 中的既有基准，不放宽容差来通过测试，不硬编码降噪，不以假数据冒充计算结果。保留 16 组 W、16 条 S 和完整交叉影响。
6. **每个代码修改批次完成后，立即同步根目录开发记录；代码和记录必须在同一个提交。** 更新当前状态、改动、验证、下一步、跨组影响及阻塞。未跑、失败、人工待测必须如实写明。
7. 收尾至少执行所属组测试、`pnpm typecheck`、`pnpm check:boundaries`；提交前执行 `pnpm check`。数值改变需完整对照。不要声称未执行的检查已通过。
8. 提交与推送前检查 git status / git diff。只提交本任务改动；不强推、不覆盖他人提交。新开发默认用所属组分支和 PR。开发记录冲突保留双方条目，重新核对顶部状态。
9. 交接报告列出修改文件、实际验证、未完成项、下一步、提交 SHA 与远端同步结果。无法推送时明确“仅本地，GitHub 尚未同步”。
10. A 组由两个独立 Codex 工作区协作。先按上述身份规则确认本次用户，再执行 `docs/coordination/README.md` 的同步和远端任务检查；读取 A1/A2 交接页，只认领所属角色的一个任务。不使用本地角色配置跳过询问。
11. main 记录已集成进度；未合并任务以原仓库的开放 PR 及其最新分支交接记录为准。只读 main 或本地日志不能判断无人开发。新功能修改前先发布任务认领提交和草稿 PR；无法发布时明确认领尚未生效，不宣称已占用远端任务。
12. B1/B2 按 `docs/coordination/B.md` 串行交接，同一时刻只有一位 B 组成员工作。两人共用现有 B 组源码、测试、日志和交接页；只分任务主责，不拆 B1/B2 专属源码目录，不把 engine/analysis 当作两人之间的编辑权限边界。A/B 组边界、共享接口协调和基准只读规则仍适用。
13. B 组每个批次同步 `docs/coordination/B.md` 和根开发记录，调试日志写明角色/执行者/任务/版本/验证结果/下一步；Git 提交正文必须带 `Role: B1` 或 `Role: B2`、`Task: <任务ID>`、`Identity-Source: user-declared`。A 组同样记录自己的角色和任务；不要追改历史提交。规则是协作约定，现有钩子和 CI 尚不验证身份声明或角色字段，不能声称已实施身份认证。

记录格式见 docs/协作开发指南.md。本地钩子通过 `pnpm setup` 安装；不要绕过记录检查。
