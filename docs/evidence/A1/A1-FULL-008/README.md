# A1-FULL-008：实时试听有界预取对照

基线为集成[PR #22](https://github.com/KnowCooker/RNC_3D_SIMULATION/pull/22)的运行源码 `d45c469224ed08abd271f8434c16963bdd0fe2c8`；候选只把A1实时拉取目标从0.4秒调整为1.2秒，每次仍请求0.2秒真实Worker样本，`LivePlayer`的2秒队列硬上限不变。A2 viewer、B算法和共享契约不变。此处的测试用浏览器真实Worker及Web Audio，通过主线程同步阻塞1.05秒模拟已观察到的长帧，不替换计算、声样或音频时钟。

[同一复现脚本](verify-real-audio-reserve.js)在 Windows 本机生产页面分别运行，原始记录为[基线](baseline.json)与[候选](candidate.json)，画面为 `baseline-after.png` 和 `candidate-after.png`：

| 观察 | 基线 | A1候选 |
| --- | --- | --- |
| 阻塞前缓冲 | 0.59秒 | 1.38秒 |
| 1.05秒阻塞后补缓冲 | 0→1 | 0→0 |
| 随后首次三维/声场操作后 | 累计2次 | 累计0次 |
| 同run真实Worker、音频时钟推进、播放/暂停/恢复 | 通过 | 通过 |
| 2秒队列上限、播放时刻及前0.5秒在4096样本快照内 | 通过 | 通过 |
| 页面/Worker异常 | 0 | 0 |

候选 `pnpm check` 通过类型、边界、67/67测试和生产构建，构建仍有大于500 kB提示。本页是受控浏览器质量对照：一次阻塞、一次视图操作的零补缓冲不证明所有设备或任意长停顿均无断续。下一步对固定候选生产版本做20分钟真实页面长稳，并在目标设备实物试听/音画验收；用户听感仍未验证。
