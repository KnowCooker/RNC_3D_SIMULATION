# 完整目标第二轮：持续仿真与剖面验证

日期：2026-09-24，Windows本机。阶段结论：持续仿真、流式试听、真实逐网格截面和全车型改制已实现并通过下列验证；完整目标尚未最终验收。

## 被测版本

本轮从任务认领提交 `79df17b` 开始；最终运行代码与根开发记录一起提交，提交可由 `git log -- src/team-b/lab/stream.ts` 定位。冻结运行源码的归一化UTF-8/LF清单摘要：

`c2e43d7994ac8941ca7f820546010e1d28a23dc3d2590d5cd90fecb9ca7f90f1`

[version.json](integration/version.json)逐文件记录53份源码/测试/包配置哈希与26份dist文件哈希；[生成脚本](integration/version.mjs)可复核。文档后续补充不改变该运行版本。正式长稳期间源码和dist冻结，不重新构建。

## 工程与数值

- `pnpm check`：59/59测试、类型、导入边界与生产构建通过；[完整日志](integration/check.log)。约547kB的Three相关脚本块提示保留。
- B组23项涵盖旧640,000样本对照及参数化源/路径/场、6项持续处理器测试。持续模式首32,000样本与批量逐样本一致；不同分块在96,000样本一致，跨过16秒继续新噪声和原权重；越环后场计算保留路径前史；数值发散后不可继续半损坏状态。
- 持续处理器使用固定大小历史，页面仅接4096样本快照。已测默认1000样本块均值9.90ms、p95 12.27ms；8参考/128系数均值28.63ms、p95 32.96ms。仅为本机计算耗时，不是端到端音画延迟或核显性能。
- A1新增8项流式播放器测试、3项Worker管理测试；A2共6项模型/截面测试。详见[持续模型说明](../../../research/rnc-stream.md)与[截面说明](../../../research/section-rendering.md)。

## 实际浏览器验证

| 范围 | 证据和结果 | 能证明什么 |
| --- | --- | --- |
| 完整生产页面持续运行 | [20/20结果](integration/final-result.json)、[脚本](integration/check.js) | 同run跨16秒、真实新源/权重、有限快照、场绝对时间、暂停停止产出/恢复、视图操作不重置、关闭RNC真实u/a为0且e=d、停止/重启、批量模式回归 |
| 独立真实Web Audio | [11项结果](audio/results.json)、[脚本](audio/check.js) | 同时钟座位/d-e切换、缓冲耗尽冻结、缺块恢复、暂停/释放与有界节点；未做物理麦克风/扬声器测量 |
| 生命周期交错 | [31项及修复后4项](lifecycle/README.md) | 实际页面/音频/物理模块配合受控Promise复现后台启动、销毁、旧场、零车速与状态标签；visibility为受控夹具，不代表操作系统节流 |
| A2模型/截面 | [独立A2报告](../../A2/A2-FULL-002/README.md) | 四类外观与座舱截图、真实三角网格封口/孔洞/开口、爆炸附着与CPU渲染耗时；常值场夹具只证明几何 |
| 完整改制与真实场组合 | [报告](modification/README.md)、[结果](modification/result.json) | 四车真实鼠标新增第5参考→5路x→点选→右键删；四门分别禁用u全样本为0；无效安装件阻止重算；12组视图/爆炸/真实场组合；19次真实计算、9次场返回、应用/Worker错误0 |
| 生产页窄窗 | [20/20结果](integration/layout-result.json)、[390px截图](integration/live-narrow.png) | 320/390/768/1440px无横向页面溢出，四误差指标、运行方式/计算/时间控件可见；零车速实时暂停状态 |
| 离线候选包 | [打包与隔离验证](offline/README.md) | 26份dist与冻结清单一致，zip内31文件校验；3次全新context、45项四车型批算/实时启动通过，外网请求0；仅同机网络隔离 |

生产流程采用独立Chromium会话及 `http://127.0.0.1:5180/` 冻结dist。完整改制使用同一冻结源码的Vite 5177，真实Worker不替换，只观察原生请求/结果和渲染对象。复现脚本使用Playwright CLI；每组先读本目录说明，将引用 `output/playwright/...` 的脚本/夹具复制到对应工作目录，启动自己的本机端口和独立浏览器。不要在同一浏览器重复安装初始化观察器。

## 保留的失败与限制

- `integration/dev-hot-reload-timeout.txt`：开发服务热更新重置运行中页面导致超时，随后改用冻结生产版。
- `integration/production-result.json`及`duplicate-observer-result.json`：重复安装Worker观察器造成同一原生事件记录两次，连续性/单次启动2项假失败。保留原始数据；独立新会话 `final-result.json` 20项全通过，未为此改变运行算法。
- 生命周期先发现“首次数据到达前隐藏，状态仍写运行”，已修复并以4项专门回归证明；原31项记录保留。
- 改制脚本首次未匹配Vite的 `?t=` URL，修订观察路由后完成；这次夹具超时未作为产品缺陷。
- 窄窗脚本首次按label精确匹配车速失败，改为按实际快照的slider角色并按Home键后通过；[原始超时](integration/layout-locator-timeout.txt)保留。
- 新版20分钟正式长稳正在执行，最终原始记录和结论另行归档。观察到瞬态音频补缓冲，时钟冻结后继续缺失样本；不提前声称零欠载或长稳通过。

当前实际渲染为RTX4070 Laptop，不能替代指定核显验收。车辆仍属详细教学模型，未达到所参照视觉精细度；[视觉差距表](../../A2/A2-FULL-002/VISUAL_GAPS.md)记录16项可执行缺口及下一独立A2任务，包含Z截面当前截图方向和Y/底视用户相机可达性问题。第二台Windows真实断网、实物听感/爆音/≤100ms音画、完整视觉逐视角验收和新版正式录像仍未完成；同机网络隔离与浏览器时间检查不能替代这些项目。

## 候选包定位

本机包 `release/lab-v3-c2e43d7994ac.zip`，3,341,221字节，SHA256 `e9d9445e9dc1b44dba65a20f2a910c8076175cdc33d5dfc4995b7b35c62c66c2`。目标机需预装Node>=22.12，包运行无需node_modules或外网。zip保存在忽略目录，不宣称已经上传GitHub Release；仓库包含制作脚本、全部结果、清单和说明，另一端可按[复现步骤](offline/README.md)从同一版本重建和验证文件内容。ZIP容器时间戳可能使重新压缩的整个包哈希不同，逐文件清单才用于内容一致性核对。
