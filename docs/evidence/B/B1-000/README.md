# B1-000 协作规则验证与交接记录

- 日期：2026-09-24。
- 用户角色：**B1**；身份来源：用户在本会话主动声明“我是B组的开发人员B1”，无需重复询问。
- 任务ID：**B1-000**；任务主责：B1；实际执行者：Codex，代B1执行。
- 接手基线：原main `d4e191b`，已从原仓库快进更新至 `1bb8a23`；任务分支：`b/b1-coordination`。
- 变更范围：协作指令、任务/交接文档、README、PR模板及开发记录；运行时源码、共享契约、依赖和数值基准未修改。
- 对应代码/文档版本：包含本记录的任务提交；最终PR HEAD确定交付版本，避免把本提交自身SHA写进自身。

## 已执行验证

| 检查 | 真实结果 |
| --- | --- |
| 同步与占用检查 | 拉取main成功；查询时原仓库开放PR为A组#23/#24，没有B组活动PR |
| 文档链接及结构 | 本轮修改的Markdown本地链接有效；B1-001～004、B2-001～004各有任务主责、验收条件及依赖 |
| 旧规则冲突检索 | 活跃根/A/B入口已移除“只需首次确定”“有本地绑定就不询问”“B1/B2分别独占engine/analysis”等旧指令 |
| `git diff --check` | 通过 |
| `pnpm check` | 通过：类型、A/B依赖边界、22/22自动测试、Vite生产构建 |
| 范围核对 | 未修改产品TypeScript/CSS、自动测试、构建配置、依赖锁文件或基准；不新增B1/B2源码目录 |

运行环境：本机Windows、Node 24.19.0、pnpm 11.19.0，使用既有依赖。测试在该环境所需的沙箱外执行；Vite约525.51kB主脚本提示仍保留，未为了文档任务改写打包策略。

## 身份流程场景核对

以下是对文档规则的审阅，不是冒充另一个真实用户执行过调试，也不是GitHub身份认证测试。

| 场景 | 必须行为 |
| --- | --- |
| 本会话用户明确“我是B1” | 记录B1后直接工作，不重复问 |
| 用户只说“继续调试”，本地缓存为B1 | 新会话没有明确身份则先问，不能用缓存代答 |
| 用户只说“我是B组” | 询问B1还是B2，回答前不调试或署名提交 |
| B2接班，上一位缓存为B1 | 采用当前用户明确B2身份；保存前任工作并更新交接，不强制拆文件 |
| 同一用户在已确认会话中继续 | 沿用已确认角色；有换人/身份歧义时重新问 |
| B2的任务需要修改engine中的数据生成 | 属于B组共用源码，可按本任务修改并记录；不因文件位于engine而禁止 |
| B2需要改变shared/integration | 仍按A1+B1共管流程协调，不能用“B组共用”越过A/B边界 |

## 交付与后续

根开发记录、本验证日志、共同交接页和本任务Git提交均记录B1与B1-000；首个提交 `8c250e4` 正文使用 `Role: B1`、`Task: B1-000`、`Identity-Source: user-declared`。已推送任务分支并创建 [PR #25](https://github.com/KnowCooker/RNC_3D_SIMULATION/pull/25)，PR同样注明身份/任务；当前尚未合入main。现有钩子与CI没有身份/角色字段验证，规则由开发者、AI指令与评审执行，不能宣称技术上已强制认证。

本次只完成协作配置，没有自动开始B1-001或B2功能任务。下一步评审合并本任务PR；B1可认领完整五算例对照，B2接班时选择源/路径核对或分析边界任务。设备、音画、第二机等原有未验收项保持真实状态。

入口：[B组共同任务表](../../../coordination/B.md)、[根开发记录](../../../../开发记录.md)。
