# A1 任务与交接

职责：页面、图表、播放器及 A 组集成。只由 A1 更新本页；先读 [协作流程](README.md)，检查原仓库开放 PR 后才能认领。main 中本页不包含尚未合并的其他分支进度。

## 当前交接

- 更新时间：2026-09-23。
- 当前任务：A1-001，进行中（先认领，再复现播放生命周期）。
- 执行者/分支/PR：本端 Codex-A1-lzhdai；a/a1-001-playback；草稿 PR 创建后补链接。
- 基线：原仓库 main 411fbed；带入协作规则 PR #1 的 9b2fb8e，合并顺序先 #1 后本任务。
- 允许修改：src/team-a/player、必要的 app 播放事件、tests/a1-*.test.ts、本人交接页/根日志及 docs/evidence/A1/A1-001。
- 已有实现：参数与重算界面、四座位 d/e 试听、统一时钟、波形/PSD/收敛图。
- 验证依据：根开发记录及 docs/验收记录.md 的初始化证据；本队列建立时未重新验证产品。
- 下一步：发布认领草稿 PR；复现自然结束后的跳转/重播、异步启动与暂停竞态、四座位及 d/e 切换，记录浏览器证据。
- 依赖/请求：无活动请求；B2 导出 API 尚待交付，A1 不代写 B2 分析与导出核心。

## 按顺序执行的队列

| ID | 状态 | 任务及完成条件 | 依赖 |
| --- | --- | --- | --- |
| A1-001 | 进行中 | 播放生命周期与公平试听：播放/暂停/重播、四座位、d/e 切换、静音和结束后重播可复现；同时间同增益；补实际操作证据与必要回归测试，实物听感未测则单列 | 可用固定参考回放，不依赖 A2 |
| A1-002 | 待认领 | 计算/取消/失败状态：计算中防重复提交；结果 runId 对应；失败保留旧实验身份且不自动冒充新实验播放；参考模式不开放无效重算选项 | A1-001；真实计算用 pnpm dev |
| A1-003 | 待认领 | 图表、选择与时间一致：点选/座位/声音模式的语义清楚，单位正确，准备中状态真实，窄窗口可操作；验证后台恢复行为 | A1-001；保持 viewer 接口 |
| A1-004 | 待认领 | 组合验收与交付：集成 A2 后记录20分钟、10次重算、20次切换；实际音画延迟与第二台Windows离线验证分别给证据或明确待测 | 对应 A2 PR 已合并；所需设备可用 |

导出入口在 B2 提供兼容 API 后再拆为独立任务；此前不阻塞 A1-001～003。每项未通过或遇到阻塞时保留真实状态，可先做后续无依赖的小任务。

## 每次交接必须填写

任务 ID / 状态 / 执行者标识 / 分支 / PR URL / 基线 SHA / 最新代码 SHA（或由 PR HEAD 定位）/ 允许修改文件 / 实际改动 / 验证命令与结果 / 可访问证据 / 未完成项 / 精确下一步 / 跨角色请求。不要把本提交自身 SHA 写进自身文件。
