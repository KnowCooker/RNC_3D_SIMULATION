# A2 任务与交接

职责：车辆、相机、标记、结构展开和道路展示。只由 A2 更新本页；先读 [协作流程](README.md)，检查原仓库开放 PR 后才能认领。main 中本页不包含尚未合并的其他分支进度。

## 当前交接

- 更新时间：2026-09-23。
- 当前任务：A2-001，待合并；本执行会话完成后停止写代码，后续成员可按本页接续。
- 执行者：Codex / root-a2-markers / 2026-09-23；用户明确授权本轮并行 A1/A2。
- 分支：a/a2-001-markers；基线：9b2fb8e（协作 PR #1）；收尾合入已发布main d4e191b（含A1-001）解决根日志冲突，不引入未合并A1-002（PR #4）。
- PR：[原仓库 #3](https://github.com/KnowCooker/RNC_3D_SIMULATION/pull/3)；最新代码 SHA 由 PR HEAD 定位；认领提交5d6bc47。
- 允许文件：src/team-a/viewer/**、tests/a2-*.test.ts、docs/evidence/A2/A2-001/**、本页、根日志 A2 状态及历史。
- 实际改动：12标签改可点选/键盘按钮，64×24目标、中文说明、选中高亮、投影防重叠和引导线；修复缩窗后画布保留旧CSS宽度。坐标、通道与声学数据未变。
- 验证：独立pnpm check通过（类型/边界、9/9测试、生产构建）；同步main d4e191b后再次19/19通过并复跑90浏览器断言。5视图×12标签点选、通道/图表、MIC座位联动、球体射线、键盘、窄窗布局；截图已检查。
- 证据：[A2-001记录及截图](../evidence/A2/A2-001/README.md)、[结果JSON](../evidence/A2/A2-001/browser-result.json)、[复现脚本](../evidence/A2/A2-001/browser-check.js)。
- 精确下一步：已核实PR #1/#2合入main（main为d4e191b）；先检查PR #3最新状态与开放A2任务。#3已同步main并保留双方根日志；A1独立组合验证。#3合并后从最新main认领A2-002，执行20轮透明/展开/复位与外部时间一致性验证。
- 依赖/请求：无 A1 接口变更请求；createViewer/render/setBody/setExploded/reset/点选回调兼容，样式及data-viewer-*均归viewer。原仓库无push权限，fork已推送、等待维护者检查并合并。
- 未完成项：目标核显性能、20轮复位、20分钟稳定性、实物听感/音画延迟仍未验收。构建既有约521kB提示与favicon404未改；后台节流自动化超时已通过独立无头复测。

## 按顺序执行的队列

| ID | 状态 | 任务及完成条件 | 依赖 |
| --- | --- | --- | --- |
| A2-001 | 待合并 | 12硬件点和标签：REF/OUT/MIC 与 fl/fr/rl/rr 正确对应，点选可用，改善遮挡；保留现有点选回调，给默认/旋转/展开截图与逐点核对证据 | 可用参考回放，不依赖 A1 |
| A2-002 | 待认领 | 透明/展开/复位稳定性：连续20轮组合操作，结构无累计漂移和丢失，复位恢复相机与结构，车轮/道路跟随传入时间 | A2-001 |
| A2-003 | 待认领 | 补四轮源独立标记与说明，清楚区分激励源和参考传感器；不得虚构额外测量信号 | 先在本页列语义提案，由 A1 协调需要的 UI/接口改动 |
| A2-004 | 待认领 | 核显性能：记录真实CPU/GPU/浏览器、渲染尺寸和测量时段，测平均帧率，按结果优化；无目标设备则记录阻塞，不用瞬时fps替代验收 | A2-002；目标设备可用 |

A2-003 等待协调时可先做 A2-004 中可完成的测量。样式限定 viewer；不修改 app/style.css 或播放器。性能优化不得改变数值、播放速度或声学路径。

## 每次交接必须填写

任务 ID / 状态 / 执行者标识 / 分支 / PR URL / 基线 SHA / 最新代码 SHA（或由 PR HEAD 定位）/ 允许修改文件 / 实际改动 / 验证命令与结果 / 可访问证据 / 未完成项 / 精确下一步 / 跨角色请求。不要把本提交自身 SHA 写进自身文件。
