# A2 任务与交接

职责：车辆、相机、标记、结构展开和道路展示。只由 A2 更新本页；先读 [协作流程](README.md)，检查原仓库开放 PR 后才能认领。main 中本页不包含尚未合并的其他分支进度。

## 当前交接

- 更新时间：2026-09-23。
- 当前任务：A2-001，进行中（先发布认领，后修改功能）。
- 执行者：Codex / root-a2-markers / 2026-09-23；用户明确授权本轮并行 A1/A2。
- 分支：a/a2-001-markers；基线：9b2fb8e（协作 PR #1）；不包含 A1-001 播放器 PR。
- PR：待本次认领提交推送后创建；代码 HEAD 由 PR 定位。
- 允许文件：src/team-a/viewer/**、tests/a2-*.test.ts、docs/evidence/A2/A2-001/**、本页、根日志的 A2 状态及历史。
- 任务/验收：核对 REF/OUT/MIC × fl/fr/rl/rr 的坐标、标签、点选；复现遮挡后修复；默认/旋转/展开逐点验证和截图；pnpm check。
- 当前验证：已读规则、计划、契约及两角色交接，原仓库开放 PR 仅 #1/#2，未发现 A2 活动认领；已离线安装冻结依赖。
- 下一步：创建认领草稿 PR 并再查重复任务，使用参考回放复现标签问题。
- 依赖/请求：协作 PR #1 待合并；现有 viewer 创建/render/控制和点选接口保持兼容，不要求 A1 改接口。原仓库无 push 权限，使用 fork PR。
- 未完成：功能、自动/浏览器验证、截图均尚未完成；实机核显与20轮稳定性属后续任务。

## 按顺序执行的队列

| ID | 状态 | 任务及完成条件 | 依赖 |
| --- | --- | --- | --- |
| A2-001 | 进行中 | 12硬件点和标签：REF/OUT/MIC 与 fl/fr/rl/rr 正确对应，点选可用，改善遮挡；保留现有点选回调，给默认/旋转/展开截图与逐点核对证据 | 可用参考回放，不依赖 A1 |
| A2-002 | 待认领 | 透明/展开/复位稳定性：连续20轮组合操作，结构无累计漂移和丢失，复位恢复相机与结构，车轮/道路跟随传入时间 | A2-001 |
| A2-003 | 待认领 | 补四轮源独立标记与说明，清楚区分激励源和参考传感器；不得虚构额外测量信号 | 先在本页列语义提案，由 A1 协调需要的 UI/接口改动 |
| A2-004 | 待认领 | 核显性能：记录真实CPU/GPU/浏览器、渲染尺寸和测量时段，测平均帧率，按结果优化；无目标设备则记录阻塞，不用瞬时fps替代验收 | A2-002；目标设备可用 |

A2-003 等待协调时可先做 A2-004 中可完成的测量。样式限定 viewer；不修改 app/style.css 或播放器。性能优化不得改变数值、播放速度或声学路径。

## 每次交接必须填写

任务 ID / 状态 / 执行者标识 / 分支 / PR URL / 基线 SHA / 最新代码 SHA（或由 PR HEAD 定位）/ 允许修改文件 / 实际改动 / 验证命令与结果 / 可访问证据 / 未完成项 / 精确下一步 / 跨角色请求。不要把本提交自身 SHA 写进自身文件。
