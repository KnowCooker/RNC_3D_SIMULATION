# B1/B2 共同任务与串行交接

本页是 B 组共用的任务表和交接入口。B1/B2 不同时工作，继续共用现有源码、测试、日志和本页；只分任务主责，不新增按人员隔离的文件或目录。A/B 两大组之间的源码边界保持不变。

依据：[B组需求](../plan/02_B组独立需求.md)、[每日任务与验收](../plan/03_每日任务与验收.md)、[固定接口](../../src/shared/contracts.ts)。以下拆分针对当前已合入 main 的 demo-v2 范围；A 组未合并草稿中的扩展不自动改变 B 组验收或契约，有跨组新需求时另行确认。

## 当前接班：B2-001（2026-09-26）

- 用户角色：B2；Identity-Source: user-declared；执行者：Codex。用户已明确确认“B1是停止的，B2继续”，本批串行接班。
- 基线：原仓库 main `a300d1631bc502c76ee6dd4f0b16990b071175dd`，B1 PR #28 已于2026-09-25合并；下方“未合并/本地未上传”为历史状态。
- 分支：`b/b2-001-source-path-audit`；任务：B2-001 合成源/路径及复现说明。状态：本地准备，远端认领尚未生效；GitHub登录尚未完成，PR未创建。
- 2026-09-26 fetch及开放PR检查：仅A1 #27，无B组活动PR。前任成果已在main；本地工作区初始干净。
- 范围：B组现有engine测试、`src/team-b/README.md`、`docs/evidence/B/B2-001/`、本页及根开发记录。验证demo-v2三种子确定性/40–350Hz整形、单位/时标、P/S因果完整矩阵和S_hat=S；区分lab-v3现状，不改冻结基准、共享接口或A组。
- 验证：基线 `pnpm check` 83/83测试、类型、边界、生产构建通过；原有大chunk提示保留。新任务证据待补。
- 下一步：发布认领草稿PR；无法认证时按协作入口的本地准备例外继续独立核对，明确未占用远端任务；完成后验证、登记并推送。

## 上一发布：B1-PR-001（2026-09-25，已合并）

角色B1；Identity-Source: local-config；执行者Codex。用户授权集中发布，代码提交8698b2c已推送origin/b/b1-coordination，[PR #28](https://github.com/KnowCooker/RNC_3D_SIMULATION/pull/28)已创建，目标main、非草稿、未合并。83项测试及类型/边界/构建通过；原始录音、MATLAB、个人本地规则未上传。下一步审核/CI；详细见[发布证据](../evidence/B/B1-PR-001/README.md)。下方本地批次为开发时历史，现已包含在PR中。

## 上一开发批次：B1-ANALYSIS-001（2026-09-25）

- 角色B1；执行者Codex；Identity-Source: local-config；3c43a99 + 未提交本地批次；仅本地、尚未远端同步或认领。
- 已完成：声压PSD默认A/可切Z，横轴默认20～500Hz可调；总级/收敛/声场默认dBA可切dB；NFxLMS联合能量归一化函数统一；可变预计算时长及播放器真实终点；教学60dBA基准/±12dB修正。
- 跨组授权：用户明确功能任务覆盖A1页面/播放器、A2声场单位及shared/integration适配；无A/B直接导入。旧demo-v2和fixture不变。用户MATLAB仍只提取数据格式，没有引入其处理算法。
- 验证：完整check，83/83测试、类型/边界/构建通过；112秒默认仿真核心约1.90秒；浏览器42秒末端、计权/频率切换、100秒实时（0补缓冲）、暂停通过。
- 限制：2kHz低频教学模型，A滤波非认证声级计；60dBA为选定教学值不是实录校准；理想S_hat=S，高μ可发散；预计算上限来自保守内存预算，不能宣称目标设备验收完成。
- 下一步：B2交接时先保留全部本地未提交文件，核对[证据](../evidence/B/B1-ANALYSIS-001/README.md)和[模型说明](../research/LAB_V3_SIGNAL_PATH_MODEL.md)；后续可做目标设备性能/真实校准或次级路径失配研究。按用户后续命令再提交上传。

## 1. 开始调试前确认身份

1. 每次新的开发/调试会话先看用户是否已明确声明本次角色。例如“我是 B1”“我本次以 B2 身份继续”。已经明确就直接记录，不重复问。
2. 未声明角色或只说“我是 B 组”时，先问：“请确认你本次是 B1 还是 B2？”收到明确回答后才调试、认领、修改、署名或提交。角色与 B 组范围无关时使用根规则的 A1/A2/B1/B2 选项。
3. `git config --local rnc.role`、GitHub 账号、Git作者、分支名、旧记录都不能代替本次身份确认。用户换人、新会话没有声明或身份有歧义时重新问；同一用户已确认身份的连续工作可以沿用。
4. 确认后可以执行 `git config --local rnc.role B1`（或 B2），只作为最近一次记录。不要将整个仓库默认绑定某个人，不修改 Git作者来假冒用户身份。
5. 认领、调试、验收、更新开发记录/日志以及提交/PR，都注明本次角色、实际执行者和任务 ID。AI 代执行时写“用户角色 B1；执行者 Codex”，而不是仅写“B组”或“AI”。

身份确认是项目中的角色声明，不是账户权限认证；现有钩子/CI不验证用户是否确实属于该角色。

## 2. 主责与共用文件

| 成员 | 任务主责 | 主要交付 |
| --- | --- | --- |
| B1 | 4×4×4 FxLMS、因果时序/矩阵、数值对照、配置/数值异常、引擎性能、B组数值基线冻结 | 算法与对照证据、异常与性能记录、供A1联调的可靠计算核心 |
| B2 | 合成激励/路径说明与核对、RMS/PSD/功率指标、结果/配方导出、数据适配、报告与交付复现 | 兼容的分析/导出API、可复现数据及操作说明、B组报告与证据索引 |

两人均可按当次任务修改 `src/team-b/**` 和 B 组既有测试。`engine` / `analysis` 仍按功能组织，不能解释为“B1不能改analysis”或“B2不能改engine”。测试保留 `tests/engine.test.ts`、`tests/analysis.test.ts` 等按功能命名，新增文件也按功能/任务需要命名，不要求 b1/b2 前缀。

`src/shared/**`、`src/integration/**`、配置和 CI 仍是 A1+B1 共管，B2 的适配任务需要这类修改时在本页写明请求，经协调确定执行范围。B 组不自行改 A 组页面/播放器，不覆盖 `fixtures/reference/**`。

## 3. 当前交接

**2026-09-25 / B1-DATA-001**：B1（Identity-Source: local-config），执行者Codex，3c43a99加未提交工作。按用户要求提取40秒/4000Hz的48参考+8耳原声（全为未经校准V），按FL49/50、FR53/54、RL51/52、RR55/56标注；MATLAB仅提供元信息，未运行或移植任何处理。用归一化谱形优化B源/初级路径，保持原本4输出S和FxLMS；8头枕实测S仅分析，原始时序留仓库外。77测试及构建通过；μ=0.5极端工况新谱下显式发散，0.08高能量/重合参考验证通过。跨组仅A1来源说明、共管B测试入口；数据说明工作原主责B2，此次由B1按用户请求执行。详见[证据/复现/限制](../evidence/B/B1-DATA-001/README.md)。本地完成后由用户检查；B2后续可核对校准、安装点及更多实录工况，未自动认领旧B2任务。未提交、未上传。

**2026-09-25 / B1-UI-002**：B1（local-config），执行者Codex；3c43a99加未提交工作。按用户最新要求，三类图各保留一张，共用图表/试听座位菜单；覆盖B1-UI-001的四座位收敛网格。保留时间/历史，切座位回到d/e对照。A1 lab局部跨组修改已获用户任务授权，无B引擎/共享接口改动。补充[lab-v3源与路径说明](../research/LAB_V3_SIGNAL_PATH_MODEL.md)（原说明主责B2，本次由B1按用户问题核对；旧B2-001仍待验收）。75项测试、类型/边界/构建通过，浏览器验证见[批次证据](../evidence/B/B1-UI-002/README.md)。仅本地未提交/未上传。下一步由用户检查交互，需要时由B2继续源/路径标定或B1/A1协调视觉路面接线；不自动认领新任务。

**2026-09-25 / B1-UI-001 本地完成、未提交**：B1（local-config），执行者Codex，按用户跨组功能请求完成四座位总声压级/原声对照、时间频率刻度和RNC ON试听开关。后续用户明确取消2秒等待：lab-v3默认0秒学习，兼容旧lab配置2秒，冻结demo-v2不改。75项自动测试、构建及回放/实时浏览器检查通过；仅在本地，未远端认领/上传。范围与验证见[本批记录](../evidence/B/B1-UI-001/README.md)。

**2026-09-25 / B1-SYNC-001**：当前由B1（身份来源local-config）授权本地合入origin/main `0fb68a7`，执行者Codex；保留当前分支与B1/B2任务拆分，未推送。新增lab-v3源码位于B组共用的lab模块，旧demo-v2队列保留为回归验收项，后续先对照新版已有能力和证据，避免重复实现；本轮不重分配或冒称完成B1/B2任务。新版检查与预览结果见根开发记录；下方2026-09-24内容保留其记录时点。

- 更新日期：2026-09-24。
- 当前用户角色：**B1**；身份来源：用户在本会话主动声明“我是B组的开发人员B1”。
- 实际执行者：Codex，代 B1 执行。
- 当前任务：**B1-000，建立B1/B2分工与身份/记录规则**；状态：**已撤回PR，保留本地成果**；B1-001～004/B2-001～004仅分配主责，尚未认领实施。
- 任务分支：`b/b1-coordination`；基线：从 `d4e191b` 快进至原仓库 main `1bb8a23`。
- 远端任务检查：本次原仓库开放 PR 为 A组 #23/#24；未见 B 组任务 PR。这是检查时点快照，下一次必须重新查询。
- 允许改动：根/A/B协作指令、README、B组共同任务表、PR模板及开发记录；此次用户已授权统一个人身份流程。无产品功能/接口/基准改动。
- 验证：文档一致性与本地链接、`git diff --check`、`pnpm check`已通过（22/22测试、类型、边界及生产构建）；原有构建体积提示仍在。证据见 [B1-000验证与交接记录](../evidence/B/B1-000/README.md)。
- PR：[原仓库 PR #25](https://github.com/KnowCooker/RNC_3D_SIMULATION/pull/25) 于2026-09-24按用户要求关闭，未合并；对应远端分支已删除。本地保留 `b/b1-coordination` 与 `61a981c`，此次状态更新未提交、未上传。
- 下次功能任务建议：B1 从 B1-001 开始；B2 接班时可从无阻塞的 B2-001/B2-002 开始。任务已分配主责，但本次不自动实施这些功能任务。
- 接班条件：目前由B1保留本地成果，下一位接班前确认B1已停止并核对本地工作区、实际分支及未提交记录，不丢弃前任改动。远端已撤回，不能从main或已关闭PR判断本地最新进展。

## 4. B1 任务队列

已有实现是起点，下面任务先核对已有证据，再补缺项，不为了“完成任务”重写正确代码。B1/B2任务 ID 与原验收 B01–B08 是两套编号。

| 任务 ID | 状态 | 工作与完成条件 | 原验收 / 依赖 |
| --- | --- | --- | --- |
| B1-001 | 待认领 | 完整数值对照：默认、seed29、seed47、taps32、μ0五算例的x/u/d/a/e逐样本对照；默认已有640000样本基准。其余Python输出写新目录；每路相对RMS≤1e-3，近零数组绝对误差≤1e-6；记录每点指标差≤0.2dB、四点改善、重复一致 | B01/B02/B03/B05；基准只读；B2协助汇总报告 |
| B1-002 | 待认领 | 完整矩阵与时序：核对W/S/P索引、16条S及12条交叉路径、e=d+a、当前误差仅更新下一样本权重、前2s关闭控制；补能发现符号/延时/交叉项错误的有效验证，保留现有脉冲测试 | B01/B06；使用B1-001对照结果 |
| B1-003 | 待认领 | 参数与数值失败：复核32/64、三种子、μ0/0.08；非法tap/NaN/固定配置异常返回明确错误；结果尺寸/有限性有效；μ0满足u=a=0及e=d；不把u限幅为1。页面保留旧实验和取消UI由A1负责 | B04/B07；沿用现有错误契约 |
| B1-004 | 待认领 | 性能与基线冻结：在约定目标设备测核心计算及Worker往返总耗时，默认16s片段目标≤5s；记录CPU/GPU/浏览器、代码SHA、配置及样本分布。确有性能失败再优化并复跑数值对照；冻结B组计算版本与未验收项 | B08/I03/I07；B1-001～003；无目标设备标待测 |

## 5. B2 任务队列

| 任务 ID | 状态 | 工作与完成条件 | 原验收 / 依赖 |
| --- | --- | --- | --- |
| B2-001 | 待认领 | 合成源/路径及复现说明：核对确定性随机源、40–350Hz整形、2000Hz/16s、固定工况和通道单位；P=4×4×24、S=4×4×16、S_hat=S，16条路径非零；说明单位比例教学映射，不能写成实车数据 | 原B需求第2/3/6节；已有数据实现可直接验证 |
| B2-002 | 待认领 | 指标与频谱边界：0.5s RMS、20μPa合成SPL、同窗功率比、四点先合功率；Hann1024/50%重叠/单边PSD归一。补窗不足、静音/近零、首尾样本、各类信号单位等边界证据，无效值不显示假0dB | 原B需求第3节；沿用analyzeAt契约；已有正弦/DC测试 |
| B2-003 | 待认领 | 结果/配方导出：提供无DOM的兼容API，导出配置、模型/路径版本、seed、runId、来源、通道顺序/单位、样本数及指标；结果数据可还原，配方可复算；对导出再读入/复算作一致性验证。格式和内存成本写清楚，禁止单独归一化信号 | B1-001、B2-002；API交给A1接UI，B组不代写页面 |
| B2-004 | 待认领 | 数据适配与最终报告：核对RunResult维度、时标、来源及分析/导出接口；整理B01～B08矩阵、固定配置、运行方法、代码SHA、真实验证、待测项与A1联调请求；协助第二台Windows离线复现。Worker/common修改须先协调，不能凭A组旧证据声称新引擎通过 | B2-001～003；汇总B1结果；设备条件不可用单列待测 |

B2-003是当前A1等待的跨组导出接口。B2可以先核对设计与格式；涉及共享契约变更必须与A1+B1协调。B1主责完整数值正确性，B2主责数据和报告整理，避免两人重复维护两套算法或两套指标公式。

## 6. 串行接班步骤

1. **身份**：采用本会话用户的明确声明；未声明先问。切换B1/B2时在本页及根日志追加接班记录。
2. **保存**：读取 `git status`、分支、开发记录、本页及原仓库开放PR。前任未提交工作保持原样，先确认归属和接续范围；不reset、不整文件覆盖、不强推。
3. **同步**：干净main可 `git pull --ff-only origin main`；已有任务分支先fetch，再按实际需要合入main。前任已推送但未合并的成果从其任务分支/PR核对，不能仅拉main后假定已接到全部工作。
4. **任务**：一次只启动一个B组任务。新功能按根规则先提交认领并建立草稿PR；B1-000是协作文档任务。接续未完成任务时保留任务ID与原主责，补实际接班人、原因和版本；不强迫B2重做B1成果。
5. **验证**：至少跑所属验证、类型与边界，提交前执行 `pnpm check`。数值改变复跑受影响算例；调试日志写明角色、执行者、任务、版本/配置、实际命令、结果与未测内容。证据统一可放 `docs/evidence/B/<任务ID>/`，按任务组织，不按B1/B2隔离。
6. **交接**：代码、根开发记录与本页同批提交；只更新日志也需要署明身份。写精确下一步、遗留项、分支/PR和最新已知SHA，推送后才称远端同步；保存交接后停止写入，由下一位接班。

状态可用：待认领、进行中、待接班、阻塞、待合并、已合并。任务完成、PR合并、设备验收三个状态分别记录。下班时的“待接班”不保证自动唤醒任何AI。

## 7. 统一记录与提交格式

每次开发记录、交接、调试/验收日志至少包含：

```markdown
### YYYY-MM-DD / B1 / B1-001 / 任务标题
- 本次用户身份：B1；来源：本会话用户主动声明（或用户回答确认）；确认日期：YYYY-MM-DD。
- 任务主责：B1；实际执行者：开发者或Codex；接班自：B2/无；分支：...；代码基线：...；PR：URL/未创建。
- 变更与配置：...
- 验证：实际命令、结果、数值容差、证据位置；未执行的明确标注。
- 当前状态：...；未完成/阻塞：...
- 下一步/交接给：具体步骤与B1/B2。
- 跨组影响：无/接口请求及对接人。
```

机器生成的原始结果可保持原格式，配套说明文件写上述元数据并指向对应证据。不得为增加署名修改冻结的数值基准或追改历史Git提交。

新Git提交示例（B2使用实际B2角色和任务）：

```text
test(b1): [B1-001] verify five reference configurations

Role: B1
Task: B1-001
Identity-Source: user-declared
```

角色与任务写在提交说明中，不依赖作者姓名或分支名暗示。PR标题含任务ID，正文写角色、身份确认来源、执行者、范围、验证和交接；合并/squash说明也保留角色与任务，历史作者不变。

## 8. 可直接使用的启动提示

```text
我是B组开发人员B1（或B2）。
先更新KnowCooker/RNC_3D_SIMULATION的最新状态，读取AGENTS.md、开发记录.md及docs/coordination/B.md。
B1/B2串行共用文件，确认前任已停止并保存工作后，继续我负责的【任务ID】。
代码、验证日志、开发记录、交接和Git提交均注明本次角色、执行者与任务ID。
如果新会话中我没有主动说明角色，先问我，不要从本地配置推断。
```
